---
// @ts-nocheck 
// Component Imports
import GContent from '../../components/GContent.astro'
import WarningBox from '../../components/Error/warning.tsx'

// Component Script:
// You can write any JavaScript/TypeScript that you'd like here.
// It will run during the build, but never in the browser.
// All variables are available to use in the HTML template below.
let title = 'Blog | Mertcan Erbaşı';
let description = "Mertcan Erbaşı | Portfolio Blog"
let allBlogPosts = Astro.fetchContent('./blog-posts/*.md');

// Full Astro Component Syntax:
// https://docs.astro.build/core-concepts/astro-components/
---
<GContent title={title} description={description}>
    {!allBlogPosts.length ? 
    <WarningBox>
        <p>Nothing here yet. (Hopefully soon 🤞)</p>
    </WarningBox>
    : 
    <div></div>}
</GContent>